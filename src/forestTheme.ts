import { createTheme } from "@mui/material/styles";

const forestTheme = createTheme({
    palette: {
        primary: {
            main: '#00A67E'
        },
        secondary: {
            main: '#1A7F64'
        },
        error: {
            main: '#D00E17'
        }
    }
});

export default forestTheme;